<!-- jakarta.persistence-api (versión 3.x) -->
<persistence xmlns="https://jakarta.ee/xml/ns/persistence"  xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="https://jakarta.ee/xml/ns/persistence https://jakarta.ee/xml/ns/persistence/persistence_3_1.xsd"
             version="3.1">
    <!-- Persistence Unit -->
    <persistence-unit name="orm_competicion" transaction-type="RESOURCE_LOCAL">

        <!-- Provider -->
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- Clase -->
        <class>model.Entrenador</class>
        <class>model.Equipo</class>
        <class>model.Estadio</class>
        <class>model.Jugador</class>
        <class>model.Partido</class>
        <class>model.Sponsor</class>
        <class>model.StatsPartido</class>

        <properties>
            <property name="jakarta.persistence.jdbc"
                      value="com.mysql.cj.jdbc.Driver"/>
            <property name="jakarta.persistence.jdbc.url"
                      value="jdbc:mysql://localhost:3306/orm_competicion?useSSL=false&amp;serverTimezone=UTC"/>
            <property name="jakarta.persistence.jdbc.user"
                      value="root"/>
            <property name="jakarta.persistence.jdbc.password"
                      value="root"/>
            <!-- Change to update if working in production -->
            <property name="jakarta.persistence.schema-generation.database.action"
                      value="drop-and-create"/>
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
            <property name="hibernate.use_sql_comments" value="true"/>
            <property name="hibernate.connection.pool_size" value="10"/>
        </properties>

    </persistence-unit>
</persistence>

